Used on my arch system, with a symlink pattern - where config files are stored here in a git repo-
and then a symlink is placed in its respective locations (typically: ~/.config)

Command usually used:
```bash
ln -s /mnt/chonker/GitRepos/dotfiles/CONFIG_DIR ~/.config/CONFIG_DIR
```
